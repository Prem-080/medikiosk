import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Bot, CalendarDays, ChevronRight, FileText, MessageCircleHeart, Play, Plus, ShieldCheck } from 'lucide-react';
import PatientShell from '../components/PatientShell';
import { api } from '../services/api';
import type { ClinicalSession } from '../types';

export default function PatientDashboard() {
  const navigate = useNavigate(); const name = localStorage.getItem('patientName') || 'Patient'; const [activeSession, setActiveSession] = useState<ClinicalSession | null>(null);
  useEffect(() => { const id = localStorage.getItem('patientId'); if (id) api.getPatientSessions(id).then(data => setActiveSession(data.sessions?.find((item: ClinicalSession) => item.status === 'active') || null)).catch(() => setActiveSession(null)); }, []);
  const answered = activeSession?.conversation.filter(message => message.role === 'patient').length || 0; const progress = Math.min(90, Math.max(15, answered * 20));
  return <PatientShell title="Your care, all in one place"><p className="text-sm font-semibold text-[#397152]">GOOD MORNING</p><h2 className="mt-2 text-4xl font-semibold tracking-[-.04em]">Hello, {name.split(' ')[0]}.</h2><p className="mt-3 max-w-xl leading-7 text-slate-600">Complete your pre-consultation case whenever you’re ready. Your practitioner will review it before your appointment.</p><section className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_.8fr]"><article className="relative overflow-hidden rounded-3xl bg-[#164d3c] p-7 text-white sm:p-9"><div className="absolute -right-16 -top-16 size-64 rounded-full border border-white/10"/><div className="relative"><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-semibold text-[#d8e8c8]"><Bot size={15}/> GUIDED CASE-TAKING</span><h3 className="mt-5 max-w-md text-3xl font-semibold tracking-[-.035em]">{activeSession ? 'Pick up where you left off.' : 'Tell us what brings you in today.'}</h3><p className="mt-3 max-w-md leading-7 text-white/75">{activeSession ? `Your answers from ${answered} step${answered === 1 ? '' : 's'} are saved securely.` : 'A few gentle questions about your symptoms, lifestyle and wellbeing—voice or touch, you choose.'}</p><button onClick={() => navigate(activeSession ? '/interview' : '/hospital-selection')} className="mt-7 inline-flex items-center gap-2 rounded-full bg-[#e7f1d5] px-5 py-3 font-semibold text-[#164d3c] transition hover:bg-white">{activeSession ? <><Play size={16}/> Continue case-taking</> : <>Start case-taking <ChevronRight size={18}/></>}</button></div></article><article className="rounded-3xl border border-[#dfe6dc] bg-white p-7"><p className="text-xs font-semibold tracking-[.14em] text-[#397152]">YOUR PROGRESS</p><div className="mt-6 flex items-end justify-between"><div><p className="text-4xl font-semibold tracking-[-.05em]">{activeSession ? `${progress}%` : '0%'}</p><p className="mt-1 text-sm text-slate-500">{activeSession ? 'Case-taking saved' : 'Case-taking completed'}</p></div><span className="grid size-12 place-items-center rounded-2xl bg-[#f2f7ee] text-[#397152]"><MessageCircleHeart size={23}/></span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#66906d] transition-all" style={{width: `${activeSession ? progress : 0}%`}}/></div><p className="mt-4 text-sm leading-6 text-slate-500">{activeSession ? 'Continue anytime—your previous answers will still be there.' : 'Your information is saved securely as you go.'}</p></article></section><section className="mt-7 grid gap-6 md:grid-cols-3"><article className="rounded-2xl border border-[#dfe6dc] bg-white p-6"><div className="flex justify-between"><span className="grid size-10 place-items-center rounded-xl bg-[#eef5ec] text-[#397152]"><CalendarDays size={19}/></span><span className="text-xs font-medium text-slate-400">UPCOMING</span></div><h3 className="mt-5 text-lg font-semibold">No consultation selected</h3><p className="mt-2 text-sm leading-6 text-slate-500">Choose your clinic and practitioner when you begin.</p></article><article className="rounded-2xl border border-[#dfe6dc] bg-white p-6"><div className="flex justify-between"><span className="grid size-10 place-items-center rounded-xl bg-[#eef5ec] text-[#397152]"><FileText size={19}/></span><span className="text-xs font-medium text-slate-400">DOCUMENTS</span></div><h3 className="mt-5 text-lg font-semibold">Keep records together</h3><button onClick={() => navigate('/reports')} className="mt-2 inline-flex items-center gap-1 text-sm font-semibold text-[#397152] hover:underline"><Plus size={15}/> Add a report</button></article><article className="rounded-2xl border border-[#dfe6dc] bg-[#f2f7ee] p-6"><ShieldCheck className="text-[#397152]" size={22}/><h3 className="mt-5 text-lg font-semibold">Practitioner-led care</h3><p className="mt-2 text-sm leading-6 text-slate-600">Medikiosk organises your answers; clinical decisions remain with your practitioner.</p></article></section></PatientShell>;
}
